# requirements.txt — sv5 (albaran-valoracion-api)
# El paquete común se instala aparte y trae httpx, sqlalchemy, pydantic Y
# los SDKs de IA (Claude/Gemini/OpenAI) con el extra [llm], porque sv5 usa
# ruesma_comun.llm.*:
#   local : pip install -e "../comun[llm]"
#   docker: lo instala el Dockerfile desde el contexto de build
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
pydantic-settings>=2.2
PyYAML>=6.0
httpx>=0.27
sqlalchemy>=2.0.30
